import React, { useState } from 'react'

function AddOrderGUI( {onReturn} ){
    return <div>
        This is an add order page
        <button onClick={onReturn}>Back</button></div>

}

export default AddOrderGUI